{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <div class="flex justify-center items-center h-screen bg-blue-200">   
        <div class="w-full max-w-md bg-gray-200 p-4 rounded-md">
            <div class="mb-4">
    <table border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>

            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>

        </tbody>
    </table>
            </div>

            <div class="mb-4">
        <table class="table">
            <thead>
            <tr>
                <th>Id</th>
                <th>Reponse</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for reponse in Listrepons %}
                <tr>
                    <td>{{ reponse.id }}</td>
                    <td>{{ reponse.reponse }}</td>
                    <td>
                        <a href="{{ path('app_reponse_show', {'id': reponse.id}) }}">show</a>
                        <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        </div>

        <a href="{{ path('app_reponse_new',{'idq': question.id}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">Create new</a></br>

    <a href="{{ path('app_question_index') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">back to list</a></br>

    <a href="{{ path('app_question_edit', {'id': question.id}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">edit</a></br>

    {{ include('question/_delete_form.html.twig') }}
</div>
</div>
    {% endblock %}
