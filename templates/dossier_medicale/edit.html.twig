{% extends 'base.html.twig' %}

{% block title %}Edit DossierMedicale{% endblock %}

{% block body %}
    <h1>Edit DossierMedicale</h1>

    {{ include('dossier_medicale/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_dossier_medicale_index') }}">back to list</a>

    {{ include('dossier_medicale/_delete_form.html.twig') }}
{% endblock %}
