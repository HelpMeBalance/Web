{% extends 'base.html.twig' %}

{% block title %}DossierMedicale index{% endblock %}

{% block body %}
    <h1>DossierMedicale index</h1>

    <div class="flex justify-center items-center h-screen bg-blue-200">   
        <div class="w-full max-w-md bg-gray-200 p-4 rounded-md">
            <div class="mb-4">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dossier_medicale in dossier_medicales %}
            <tr>
                <td>{{ dossier_medicale.id }}</td>
                <td>
                    <a href="{{ path('app_dossier_medicale_show', {'id': dossier_medicale.id}) }}">show</a>
                    <a href="{{ path('app_dossier_medicale_edit', {'id': dossier_medicale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
    <a href="{{ path('app_dossier_medicale_new') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">Create new</a>

    </div>
</div>
    {% endblock %}
